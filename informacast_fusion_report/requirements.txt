requests>=2.31
jinja2>=3.1
python-docx>=1.1
python-dotenv>=1.0
# Optional, only needed for --format pdf.
# WeasyPrint also needs system libraries (Pango/Cairo) — see
# https://doc.courtbouillon.org/weasyprint/stable/first_steps.html#installation
weasyprint>=61 ; extra == "pdf"
